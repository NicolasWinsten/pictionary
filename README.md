# Pictionary

Spring Boot + Alpine.js example.

## Run

```bash
./mvnw spring-boot:run
```

If you do not have the Maven wrapper yet, run:

```bash
mvn -q -DskipTests package
mvn spring-boot:run
```

Open http://localhost:8080/ in a browser.